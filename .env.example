# Server
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Authentication (disabled for local dev)
AUTH_DISABLED=true

# LLM API Keys (at least one required for AI features)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# LLM Provider (optional - defaults to 'anthropic' if ANTHROPIC_API_KEY is set)
# Options: 'anthropic' or 'openai'
LLM_PROVIDER=anthropic

# RapidAPI Key (for JSearch job search API)
RAPIDAPI_KEY=

# Database (SQLite for local dev)
DATABASE_PATH=./data/local.db
