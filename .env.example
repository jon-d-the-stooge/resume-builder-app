# Server
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Authentication
# Set AUTH_DISABLED=true to bypass JWT validation (development only)
AUTH_DISABLED=true

# Auth0 Configuration (required when AUTH_DISABLED=false)
# Get these values from your Auth0 dashboard:
# 1. Create an API in Auth0 Dashboard > Applications > APIs
# 2. AUTH0_DOMAIN: Your tenant domain (e.g., 'your-tenant.auth0.com')
# 3. AUTH0_AUDIENCE: The API identifier you set when creating the API
AUTH0_DOMAIN=
AUTH0_AUDIENCE=

# LLM API Keys (at least one required for AI features)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# LLM Provider (optional - defaults to 'anthropic' if ANTHROPIC_API_KEY is set)
# Options: 'anthropic' or 'openai'
LLM_PROVIDER=anthropic

# RapidAPI Key (for JSearch job search API)
RAPIDAPI_KEY=

# Database (SQLite for local dev)
DATABASE_PATH=./data/local.db
