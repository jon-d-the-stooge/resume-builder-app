/**
 * Jobs routes - handles job posting related operations.
 * Endpoints for managing job listings, job details parsing,
 * and job-to-resume matching functionality.
 */

import { Router } from 'express';

const router = Router();

export default router;
