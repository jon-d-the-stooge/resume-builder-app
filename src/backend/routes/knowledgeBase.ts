/**
 * Knowledge Base routes - handles knowledge base document operations.
 * Endpoints for managing professional experience documents, skills,
 * achievements, and other resume-building content sources.
 */

import { Router } from 'express';

const router = Router();

export default router;
