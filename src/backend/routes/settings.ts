/**
 * Settings routes - handles application configuration.
 * Endpoints for managing user preferences, AI model settings,
 * default templates, and other application-wide configuration.
 */

import { Router } from 'express';

const router = Router();

export default router;
