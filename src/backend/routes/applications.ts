/**
 * Applications routes - handles job application tracking.
 * Endpoints for managing application status, tracking submissions,
 * and maintaining application history and notes.
 */

import { Router } from 'express';

const router = Router();

export default router;
